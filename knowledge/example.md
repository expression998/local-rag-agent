# 示例知识文档

这是一个可以提交到 GitHub 的小型示例文档，用来验证 RAG Agent 的索引和问答流程。

## 项目说明

RAG Agent 会读取 `knowledge/` 目录中的 `.md`、`.txt`、`.pdf` 和 `.docx` 文件，将内容切分为文本片段，并写入本地 ChromaDB 向量数据库。

## 使用建议

真实知识库文件通常包含私有内容或版权内容，默认不会提交到 GitHub。发布项目时，请只保留可公开的示例文件。

## 测试问题

你可以启动项目后提问：

```text
RAG Agent 支持哪些知识文件格式？
```
