# 更新日志

本文件记录每个发布版本的显著变更。

## 未发布

### 新增功能

- **界面配置供应商 API**：左上角"模型连接"面板支持直接填写 API Key 与接口地址（可选），保存后立即生效，本机持久化于 `provider_settings.json`（已 gitignore，内含明文密钥绝不提交），CLI 同样读取该配置
- **动态模型列表**：保存配置后自动从供应商的 OpenAI 兼容 `/models` 接口拉取真实可用模型（`GET /api/models` 可单独刷新），替代内置写死的清单；拉取失败时回退到静态清单并提示原因
- Key 以掩码展示（仅露末 4 位），已配置时输入框留空表示保持不变

## 0.4.1 - 2026-09-18

### 修复

- **拖拽遮罩常驻遮挡页面**：`.drop-overlay` 的 `display: grid` 覆盖了 `hidden` 属性的默认隐藏样式，导致页面一打开就被"松开以上传"遮罩盖住且无法关闭。补上 `.drop-overlay[hidden] { display: none }` 恢复隐藏语义，遮罩现在只在拖入文件时出现。

## 0.4.0 - 2026-09-18

### 新增功能

- **文档上传**：知识库面板"上传文档"按钮 + 整页拖拽上传；`POST /api/upload`（原始字节 + `X-Filename` 头），扩展名白名单、20MB 上限，入库后自动增量索引
- **文档删除**：文件行删除按钮（带确认），`POST /api/knowledge/delete` 同步清理向量片段与清单；文件名校验防路径穿越
- **会话持久化与多会话**：新增 `sessions.py`（SQLite，标准库）；对话落盘重启可恢复，顶栏下拉切换历史会话、"新对话"开始新会话；内存侧保留 LRU 上限
- **引用片段展开**：点击来源卡片展开/收起全文
- **深色模式**：顶栏一键切换，localStorage 持久化，首次访问跟随系统偏好

### 工程化

- **重依赖懒加载**：torch/chromadb 延迟到引擎实例化时导入，CLI `--help` 与测试不再拉起科学计算栈（pytest 全套 1.3s）
- **测试**：新增 pytest 套件（切块/RRF/清单/查询改写/Prompt/会话存储，32 项）与 Node 渲染器测试（XSS/链接白名单/引用转换等 7 项）
- **CI**：GitHub Actions——Python 语法检查 + pytest + 前端语法与渲染器测试
- **日志**：引擎与 Web 日志改用 `logging`（含时间戳与级别），CLI 交互输出保持 print

## 0.3.0 - 2026-09-18

### 稳定性

- **会话内存上限**：Web 端会话改为 LRU 淘汰（最多 128 个），修复长时间运行内存只增不减
- **BM25 索引缓存**：检索不再每次查询全量拉取知识库重建 BM25（大知识库下查询耗时显著下降）；同时过滤零分结果，无关片段不再进入融合
- **编码兼容**：`.md`/`.txt` 按 `utf-8-sig → gb18030` 顺序尝试解码，GBK 文档可直接索引；无法解码的文件跳过并告警，不再中断整个索引流程
- **删除文件自动清理索引**：知识目录中已删除的文件，其向量片段在下次索引时自动移除
- **断连处理**：浏览器中断流式请求时服务端正确收尾，不再抛未处理异常；半截回答不进入会话历史
- **LLM 请求超时**：客户端统一 120s 超时，供应商无响应不再挂死线程
- **空历史跳过改写**：会话第一问不再发起多余的查询改写 LLM 调用（省一次往返的延迟与费用）
- **线程安全**：索引构建/重建与检索查询互斥，Web 并发下不再竞态

### 检索质量

- **切块重叠**：相邻块之间保留约 80 字符的重叠（按整句对齐，`CHUNK_OVERLAP` 可调），缓解长文档跨块信息丢失
- **嵌入归一化**：文档与查询向量统一 L2 归一化，内积检索等价余弦相似度（与嵌入模型训练目标一致）
- **索引版本迁移**：清单新增版本字段，升级后自动触发一次全量重建（首次启动较慢属预期）

## 0.2.0 - 2026-09-14

### 新增

- **Web 界面**：`uv run web.py` 启动，浏览器访问 <http://127.0.0.1:8000>
  - 三栏工作台：模型连接与知识库面板、对话区、引用来源面板
  - NDJSON 流式输出，等待时有打字动画，生成中有光标跟随
  - 回答支持安全 Markdown 渲染（标题/列表/代码块/粗斜体等；全量 HTML 转义，链接仅放行 http/https/mailto）
  - 回答中的 `[来源: 文件 - 片段 N]` 引用可点击，联动高亮右侧对应的来源卡片
  - 欢迎页提供可点击的快捷问题卡片
  - 知识库面板展示文件列表（按类型着色徽标）、文件/片段统计，支持一键重建索引
- **多轮对话查询改写**（`query_rewrite.py`）：LLM 结合对话历史把追问（如"那第二种呢？"）补全为独立检索词，与原始问题各自检索后 RRF 融合，提升多轮场景召回。可通过 `ENABLE_QUERY_REWRITE=false` 关闭，详见 README

### 修复

- `rebuild_full.py --test-query` 对 Chunk 对象做切片导致的 TypeError
- Web 端长对话把页面整体撑高、无法滚动的问题（grid 子项缺 `min-height: 0`）
- 超长文件名的引用标签溢出对话气泡（中间截断显示，悬停可见全名）

### 其他

- 静态资源响应增加 `Cache-Control: no-cache`，避免浏览器使用过期前端文件
- `.gitignore` 补充 `.uv-cache/`、`.agents/`
- README 补充 Web 启动方式、查询改写说明与项目结构
- 移除内部交接文档与私有知识文档的版本跟踪（本地文件保留）

## 0.1.0 - 2026-06-22

- 首个版本：CLI 交互问答、混合检索（向量 + BM25 + RRF 融合）、CrossEncoder 重排序（缺失时自动降级）、基于 mtime 的增量索引、多供应商 LLM 支持（DeepSeek / Moonshot / Qwen / MiniMax / OpenRouter / 本地代理）
